import XCTest
@testable import StoryboardRoutePackage_Lib

final class StoryboardRoutePackage_LibTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(StoryboardRoutePackage_Lib().text, "Hello, World!")
    }
}
